import Service from '@ember/service';
// import Ember from '@Ember';



export default Service.extend({
    checkSession() {
        // let request = Ember.$.get("/checkSession")
        // request.done(result => {
        //     this.set("isLoggedIn", true)
        // }).fail(() => {
        //     this.set("isLoggedIn", false)
        // })
    }
});
